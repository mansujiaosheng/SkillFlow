---
name: 通用项目 Skill 路由
description: 当用户提出软件项目开发、重构、协作、Git、测试、CI、接口契约、AI 协作、PR 审核、依赖安全、治理文档等综合问题时调用；用于按任务类型选择相关 Skill，不包含项目专属规则。
---

# 通用项目 Skill 路由

## 通用执行约束

- 本 Skill 面向任意软件项目、数据项目、自动化项目或 AI 协作项目，不绑定具体业务、仓库名、目录名、团队名或技术栈。
- 回答或修改前，必须先基于当前仓库的真实文件、用户本次目标和已有约定判断；禁止臆造不存在的目录、接口、配置、数据或团队规则。
- 用户指令与仓库事实、公开接口、安全底线、可回滚原则或已明确的验收标准冲突时，必须先指出冲突点和风险，再给出更稳妥的替代方案。
- 用户坚持继续时，也只能执行可回滚、可测试、不会破坏公开接口和安全底线的部分；禁止删除测试、伪造结果、绕过保护机制或扩大任务范围。
- 多个方案都可行时，必须选择影响范围更小、依赖更少、测试更明确、回滚更容易的方案。

## 功能定位

用于把用户需求拆成适合的工作类型，并判断应参考哪些同级 Skill。它只做意图分流和执行框架，不引入任何具体业务背景。

## 不触发场景

- 用户只问纯概念，且不要求落到项目流程、代码修改、PR、测试或文档时，不套用完整项目流程。
- 用户明确只要求某个单点答案时，不展开无关 Skill。

## 路由规则

| 用户意图 | 相关 Skill |
|---|---|
| 恢复项目背景、目录、架构、上下文 | `01-project-context` |
| 多人协作、排期、分工、联调 | `02-team-workflow` |
| 分支、commit、push、PR、冲突 | `03-git-workflow` |
| 环境初始化、依赖、pre-commit、本地检查 | `04-env-bootstrap` |
| 接口字段、数据模型、模块间协议 | `05-interface-contract` |
| 让 AI 写代码、限制范围、验收 AI 输出 | `06-ai-collaboration` / `21-ai-task-prompt-template` |
| 代码风格、目录归属、命名、禁止事项 | `07-code-style` / `17-module-boundary` / `18-naming-registry` |
| 测试、CI、合并前自查 | `08-testing-ci-pr` |
| 开发日志、PR 总结、周会同步 | `09-devlog-pr-summary` |
| 分支保护、Review 规则、合并限制 | `10-branch-protection` |
| 数据解析、特征工程、ETL | `11-data-processing-feature-engineering` |
| 规则、策略、阈值、告警证据 | `12-rule-engine` |
| 机器学习、模型训练、评估、推理 | `13-ml-engine` |
| 多信号融合、评分、排序、等级 | `14-scoring-engine` |
| 看板、报表、可视化、导出 | `15-dashboard-visualization` |
| 项目整体体检、PR 综合检查 | `16-project-checker` / `22-ai-pr-review` |
| 任务登记、防重复开发 | `19-task-registration` |
| Registry、Schema、文档、测试同步 | `20-registry-schema-sync` |
| 依赖、安全风险、敏感信息 | `23-dependency-security-review` |
| 自动检查脚本、CI 扩展 | `24-automation-check-scripts` |
| README、AGENTS、PR 模板、治理文档 | `25-project-governance-docs` |

## 执行方式

1. 先识别用户要解决的问题类型。
2. 读取或要求用户提供与任务直接相关的项目事实。
3. 只采用与当前任务相关的规则，不一次性套用所有 Skill。
4. 如果任务跨多个领域，合并相关 Skill 的检查项，但不得引入项目外假设。
5. 输出结论、执行步骤、风险和必要检查命令。

## 通用输出要求

- 先给结论，再给步骤。
- 涉及命令时，给出可直接复制的命令。
- 涉及代码改动时，说明影响范围、需要补充的测试、是否需要更新文档或变更记录。
- 信息不足时，指出需要读取的项目文件或需要确认的事实；不得用猜测替代项目事实。
