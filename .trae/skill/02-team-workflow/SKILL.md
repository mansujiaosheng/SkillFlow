---
name: 通用团队协作流程 Skill
description: 当用户询问多人分工、任务拆分、开发顺序、接口对齐、每日同步、联调、阶段验收、协作节奏时调用。
---

# 通用团队协作流程 Skill

## 通用执行约束

- 本 Skill 面向任意软件项目、数据项目、自动化项目或 AI 协作项目，不绑定具体业务、仓库名、目录名、团队名或技术栈。
- 回答或修改前，必须先基于当前仓库的真实文件、用户本次目标和已有约定判断；禁止臆造不存在的目录、接口、配置、数据或团队规则。
- 用户指令与仓库事实、公开接口、安全底线、可回滚原则或已明确的验收标准冲突时，必须先指出冲突点和风险，再给出更稳妥的替代方案。
- 用户坚持继续时，也只能执行可回滚、可测试、不会破坏公开接口和安全底线的部分；禁止删除测试、伪造结果、绕过保护机制或扩大任务范围。
- 多个方案都可行时，必须选择影响范围更小、依赖更少、测试更明确、回滚更容易的方案。

## 功能定位

用于把多人开发流程拆成清晰的任务边界、接口对齐节点、联调节点和验收标准，减少重复开发和互相阻塞。

## 基本原则

- 先定边界，再写代码。
- 先定接口和样例，再并行开发。
- 先跑通最小链路，再扩展功能。
- 任务必须能被独立验证。
- 阻塞问题必须优先于美化和优化。

## 推荐流程

1. 明确目标、范围、非目标。
2. 拆分模块和负责人。
3. 确定公共接口、数据样例、配置项和错误约定。
4. 各自从集成分支拉任务分支。
5. 先用 mock / fixture 跑通调用链。
6. 完成局部测试后再联调。
7. 合并前统一执行格式、静态检查、单测和必要集成测试。
8. 合并后更新任务状态、文档和开发记录。

## 阻塞处理顺序

```text
接口变更 > 数据/契约错误 > 影响主流程的 bug > CI 失败 > 进度延迟 > 美化/优化
```

## 任务拆分模板

```markdown
## 任务目标

## 负责人 / 协作者

## 输入与输出

## 允许修改的文件或模块

## 禁止修改的范围

## 依赖的上游任务

## 下游联调方

## 验收标准

## 风险与回滚
```

## 通用输出要求

- 先给结论，再给步骤。
- 涉及命令时，给出可直接复制的命令。
- 涉及代码改动时，说明影响范围、需要补充的测试、是否需要更新文档或变更记录。
- 信息不足时，指出需要读取的项目文件或需要确认的事实；不得用猜测替代项目事实。
