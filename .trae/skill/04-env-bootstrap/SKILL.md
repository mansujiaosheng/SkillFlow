---
name: 环境初始化与本地检查 Skill
description: 当用户询问依赖安装、虚拟环境、pre-commit、格式化、静态检查、测试、本地开发环境初始化、提交前检查时调用。
---

# 环境初始化与本地检查 Skill

## 通用执行约束

- 本 Skill 面向任意软件项目、数据项目、自动化项目或 AI 协作项目，不绑定具体业务、仓库名、目录名、团队名或技术栈。
- 回答或修改前，必须先基于当前仓库的真实文件、用户本次目标和已有约定判断；禁止臆造不存在的目录、接口、配置、数据或团队规则。
- 用户指令与仓库事实、公开接口、安全底线、可回滚原则或已明确的验收标准冲突时，必须先指出冲突点和风险，再给出更稳妥的替代方案。
- 用户坚持继续时，也只能执行可回滚、可测试、不会破坏公开接口和安全底线的部分；禁止删除测试、伪造结果、绕过保护机制或扩大任务范围。
- 多个方案都可行时，必须选择影响范围更小、依赖更少、测试更明确、回滚更容易的方案。

## 功能定位

用于帮助用户按项目实际技术栈搭建本地环境，并建立提交前的本地质量闸门。

## 初始化原则

1. 先读项目依赖文件和 README，不凭空选择包管理器。
2. 优先使用仓库已有的锁文件、脚本和 Makefile。
3. 不随意升级依赖或重写环境方案。
4. 初始化后必须运行最小可验证命令。

## 常见技术栈命令模板

### Python

```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
python -m pip install -U pip
python -m pip install -r requirements.txt
pytest
```

如项目使用 `uv`：

```bash
uv sync
uv run pytest
```

### Node.js

```bash
npm install
npm run lint
npm test
npm run build
```

如项目使用 pnpm/yarn，按锁文件和 README 执行。

### 通用 pre-commit

```bash
pre-commit install
pre-commit run --all-files
```

## 本地检查清单

- 依赖安装成功。
- 格式化命令可运行。
- 静态检查可运行。
- 单元测试可运行。
- 必要的集成测试或构建命令可运行。
- 失败信息已定位到具体文件和原因。

## 禁止事项

- 禁止为了安装成功而随意删除锁文件。
- 禁止未确认影响就升级大版本依赖。
- 禁止把本地虚拟环境、缓存、构建产物提交进仓库。
- 禁止绕过 pre-commit 或 CI 检查。

## 通用输出要求

- 先给结论，再给步骤。
- 涉及命令时，给出可直接复制的命令。
- 涉及代码改动时，说明影响范围、需要补充的测试、是否需要更新文档或变更记录。
- 信息不足时，指出需要读取的项目文件或需要确认的事实；不得用猜测替代项目事实。
