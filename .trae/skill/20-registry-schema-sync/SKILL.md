---
name: Registry Schema 文档同步 Skill
description: 当用户新增可发现能力、修改输出字段、配置字段、CLI/API 参数、schema、注册表、插件入口、文档或测试同步关系时调用。
---

# Registry Schema 文档同步 Skill

## 通用执行约束

- 本 Skill 面向任意软件项目、数据项目、自动化项目或 AI 协作项目，不绑定具体业务、仓库名、目录名、团队名或技术栈。
- 回答或修改前，必须先基于当前仓库的真实文件、用户本次目标和已有约定判断；禁止臆造不存在的目录、接口、配置、数据或团队规则。
- 用户指令与仓库事实、公开接口、安全底线、可回滚原则或已明确的验收标准冲突时，必须先指出冲突点和风险，再给出更稳妥的替代方案。
- 用户坚持继续时，也只能执行可回滚、可测试、不会破坏公开接口和安全底线的部分；禁止删除测试、伪造结果、绕过保护机制或扩大任务范围。
- 多个方案都可行时，必须选择影响范围更小、依赖更少、测试更明确、回滚更容易的方案。

## 功能定位

用于保证“代码能力、注册入口、schema、配置、文档、测试”同步，防止能力实现了但不可发现，或字段改了但文档和测试仍是旧的。

## 什么变更必须同步

- 新增命令、插件、任务、规则、模型、处理器、页面、导出格式。
- 修改 API / CLI 参数。
- 修改配置字段、环境变量、默认值。
- 修改输入输出 schema。
- 修改可被外部调用或自动发现的注册项。
- 修改文档中承诺的能力。

## 检查流程

1. 找到实际实现。
2. 找到注册入口或导出入口。
3. 找到 schema、类型定义或接口文档。
4. 找到测试和 fixtures。
5. 找到用户文档和开发文档。
6. 判断是否需要迁移说明。

## 输出模板

```markdown
## 同步结论

## 实现位置

## Registry / 导出入口

## Schema / 类型 / 配置

## 文档

## 测试

## 风险与迁移
```

## 通用输出要求

- 先给结论，再给步骤。
- 涉及命令时，给出可直接复制的命令。
- 涉及代码改动时，说明影响范围、需要补充的测试、是否需要更新文档或变更记录。
- 信息不足时，指出需要读取的项目文件或需要确认的事实；不得用猜测替代项目事实。
